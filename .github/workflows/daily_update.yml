name: bbb Auto System

on:
  schedule:
    - cron: '0 9 * * *' # 每天下午 5 點
  workflow_dispatch: # 手動按鈕
  repository_dispatch: # 接收網頁訊號
    types: [add_stock_trigger]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install yfinance google-generativeai pandas

      - name: Run Update Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        # 如果是網頁觸發的，就傳入 --add 參數；否則就直接跑 (更新現有)
        run: |
          if [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            echo "🔥 觸發新增股票: ${{ github.event.client_payload.stock_id }}"
            python app.py --add "${{ github.event.client_payload.stock_id }}"
          else
            echo "🔄 執行例行更新"
            python app.py
          fi

      - name: Commit and Push
        run: |
          git config --global user.name "bbb-bot"
          git config --global user.email "bot@github.com"
          git add data.json
          git commit -m "📈 Data updated" || exit 0
          git push
